[defaults]
inventory = inventory.yml
roles_path = roles
host_key_checking = False
stdout_callback = yaml
retry_files_enabled = False
# Provisioning is delegated to the Proxmox API on localhost; enclave hosts are not SSH-reachable.

[ssh_connection]
pipelining = True
